parameters:
  projectName: ''

variables:
  helmVersion: 3.4.0
  registryServerName: '$(registryName).azurecr.io'
  projectName: ${{ parameters.projectName }}
  imageName: ${{ parameters.projectName }}
  imageTag: $(build.buildId)
  chartName: fluxapp
  helmChartVersion: v1.0.0
