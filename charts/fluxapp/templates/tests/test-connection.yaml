apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluxapp.fullname" . }}-test-connection"
  labels:
    {{- include "fluxapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluxapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
