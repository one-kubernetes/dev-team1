apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxapp.fullname" . }}
  labels:
    {{- include "fluxapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluxapp.selectorLabels" . | nindent 4 }}
